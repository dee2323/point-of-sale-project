import React from 'react'
import Layout from '../../layout'
import POS from './POS'

const POSPage: React.FC = () => {
    return (
        <Layout>
            <POS />
        </Layout>)
}

export default POSPage